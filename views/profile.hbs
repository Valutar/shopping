<h1>This is the Profile Page</h1>
<h3>Hi {{user.username}}</h3>
<br />
<a href='/explore'>Explore other tweets</a>
<br />
<h3>Add a tweet:</h3>
<form action='/profile/tweet/new' method='POST'>
  <label for='tweet'>What you'd like to share:</label>
  <br />
  <textarea
    name='tweet'
    id='tweet'
    cols='30'
    rows='10'
    maxlength='280'
    required
    autofocus
  ></textarea>
  <button type='submit'>Post!</button>
</form>
<br />
{{! The user's tweets are displayed here }}
{{#each tweets}}
  <div>
    <h4>Posted by: {{author.username}}</h4>
    <p>{{content}}
      <br />
      {{! Checks if tweet was edited and returns latest timestamp }}
      <small>{{normalDate createdAt updatedAt}}</small>
      {{#ifTwoParams author._id ../currentUser}}
        <br />
        <a href='/profile/tweet/del/{{_id}}' data-method='delete'>❌ Delete this
          tweet</a>
        <br />
        <a href='/profile/tweet/edit/{{_id}}'>✏ Edit this tweet</a>
        {{else}}
        <br>
        <a href="/profile/tweet/remove-retweet/{{_id}}">❌🔁 Remove retweet</a>
      {{/ifTwoParams}}
    </p>
    <br />
  </div>
{{/each}}
<a href='/logout'>Log out</a>